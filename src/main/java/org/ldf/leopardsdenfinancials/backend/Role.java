package org.ldf.leopardsdenfinancials.backend;

/**
 * Enum representing roles for users in the application.
 * Roles determine the level of access and permissions.
 */
public enum Role {
    /**
     * Regular user role.
     */
    USER,

    /**
     * Administrator role with elevated privileges.
     */
    ADMIN
}
